###############################################
# MoveIt Servo config for VAM inference on TM12S
# FALLBACK: Uses JointTrajectory output type
#
# Use this if ForwardCommandController is NOT available with tm2_ros2.
# This publishes trajectory_msgs/JointTrajectory to the arm controller.
###############################################
use_gazebo: false

## Properties of incoming commands
command_in_type: "speed_units"
scale:
  linear:  0.6
  rotational:  0.3
  joint: 0.01

system_latency_compensation: 0.04

## Properties of outgoing commands
publish_period: 0.004  # 250 Hz
low_latency_mode: false

# FALLBACK: JointTrajectory output instead of Float64MultiArray
command_out_type: trajectory_msgs/JointTrajectory

publish_joint_positions: true
publish_joint_velocities: false
publish_joint_accelerations: false

## Smoothing filter
smoothing_filter_plugin_name: "online_signal_smoothing::ButterworthFilterPlugin"

is_primary_planning_scene_monitor: false

low_pass_filter_coeff: 2.0

## MoveIt properties
move_group_name: tmr_arm
planning_frame: base

## Other frames
ee_frame_name: flange
robot_link_command_frame: flange

## Stopping behaviour
incoming_command_timeout: 0.1
num_outgoing_halt_msgs_to_publish: 4

## Singularity and joint limits
lower_singularity_threshold: 100.0
hard_stop_singularity_threshold: 200.0
joint_limit_margin: 0.1

## Topic names
cartesian_command_in_topic: ~/delta_twist_cmds
joint_command_in_topic: ~/delta_joint_cmds
joint_topic: /joint_states
status_topic: ~/status
# Publish to the arm controller's trajectory topic
command_out_topic: /tmr_arm_controller/joint_trajectory

## Collision checking
check_collisions: true
collision_check_rate: 5.0
collision_check_type: threshold_distance
self_collision_proximity_threshold: 0.01
scene_collision_proximity_threshold: 0.02
collision_distance_safety_factor: 1000.0
min_allowable_collision_distance: 0.01
